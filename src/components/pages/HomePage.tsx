import { MainContainer } from "..";
import Header from "../Header/Header";

const HomePage = () => {
  return (
    <MainContainer>
      <Header />
    </MainContainer>
  );
};

export default HomePage;
